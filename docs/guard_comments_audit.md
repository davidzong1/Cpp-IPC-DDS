# `⛔` 注释审计记录(2026-09-20)

> 范围: 仓库内全部 `*.cc / *.cpp / *.h / *.hpp`(不含 `docs/`、`3rdparty/`、`build/`)。
> 方法: 逐处提取上下文,对照当前代码现状与台账(`unfixed_defects.md`)/修复文档归因。

## 1. 总结论

共 **151 处**(53 个文件)。审计确认:**本仓的 `⛔` 语义是"防复发守则 / 边界警示 / 变更禁令",不是"缺陷存续标记"**。绝大多数 ⛔ 描述的是"这段代码为什么必须这样写"的活态契约,删除它们会直接制造回归风险。按三分法处置:

| 类别 | 判定标准 | 数量 | 处置 |
|---|---|---|---|
| A | 描述的缺陷**已修复**,注释是修复前叙事,删除无信息损失 | **7** | 删除/压缩(见 §2) |
| B | 问题**仍然存在**(含台账已登记项在源码内的落点) | **1** | 注释保留,条目记入 §3(3.1 已于 2026-09-20 收口, 记录保留) |
| C | 防复发守则/边界警示/契约声明(非缺陷) | **142** | 保留(§4 说明理由) |

## 2. A 类:缺陷已修复,注释已删除或压缩(7 处)

原则:去掉"修复前叙事"(消除"缺陷还在"的误读),保留一句现状契约(防止回退)。原始叙事在对应 docs/ 修复记录里均有存档。

| 位置 | 原缺陷(已修) | 处置 |
|---|---|---|
| `include/ipc_msg/ipc_msg_base/udp_id_init_msg.hpp` 头部 | 文件缺 include guard,被多 TU 同时包含即 redefinition(2026-09 已补 guard) | **整段删除**(guard 已在,叙事无存量价值) |
| `src/libipc/ipc.cpp` `make_cache` | 分配失败 nullptr 直达 memcpy(订阅线程单 ip 崩溃,UF-002 族) | 压缩:删崩点叙事,保留"返回空 buff_t 与其余失败出口一致"契约 |
| `src/dzIPC/socket_ser_cli_ipc.cc` StopHS 分支 | 旧实现 StopHS 从不发送帧 ⇒ 路径裁定信号到不了对端 | 压缩:保留"StopHS 周期性发帧 + run_status 语义不变"契约 |
| `src/dzIPC/socket_ser_cli_ipc.cc` 客户端构造 | `domain_id_` 从未赋值 ⇒ 池条目 domain 恒 0 ⇒ 同机判定永不成立 | 压缩:保留"必须接形参 + 漏接后果"契约 |
| `src/dzIPC/socket_ser_cli_ipc.cc` 握手发送 | 重试耗尽仍无条件置握手完成 ⇒ 静默失败族 | 压缩:保留"只有确实发出才置完成"契约 |
| `src/dzIPC/auto_ser_cli_ipc.cc` 建腿等待 | 旧路径无限期等控制面 Ready(静默挂起) | 压缩:保留"有界、有终态、有计数"契约 |
| `src/dzIPC/auto_ser_cli_ipc.cc` 对端撤销分支 | F2 前"所有原因都写死 ChannelOccupied"(错误归因) | 压缩:保留"照对端给出的原因记账 + 分支位置约束" |

## 3. B 类:问题仍然存在(源码内有 ⛔ 落点)

### 3.1 UF-004 有条件修复的保留面 —— `src/dzIPC/dzipc.cc`(超时收尾)

> "⛔ 栈不展开 ⇒ main 里的实例不析构 ⇒ 段不 unlink —— 残留语义与改前一致,这是'有条件修复'的保留面。"

- 状态(2026-09-20 更新):**已收口** —— 原 ⛔ 注释已改写为收口态契约, 两条 exit 路径(超时/RequestShutdown)接入 `shm::unlink_created_segments()` 名字级扫除; 修法/判据/指纹见 `uf004_timeout_unlink_fix.md`(test_uf004_timeout_unlink 2/2 × 3 轮)。本条目由"活态限制"转为历史记录保留。
- 原"注释必须保留"的理由(标记活态残留)已随收口消解; 台账 UF-004 🔶 其余子项(OnShutdown/退出码/CleanupIpcInstances 实例析构)仍未修, 但源码内无对应 ⛔ 落点。

### 3.2 UF-011 池空闲链二次入池 —— `src/libipc/ipc.cpp`(note_double_return 守卫)

- 状态:修复**进行中**(归还幂等守卫已落码但未提交;台账 ⏸️ 判据构建待排)。
- `ipc.cpp:607/750` 的 ⛔ 是守卫的**射程声明**(对 ABA 射程为零)——经核实,ABA 一侧的防护(`prod_cons.h` pop() 的 epoch 比对,`:249/:267-268`)**已实施**,故 ABA 不构成活缺陷;真正的活项是 UF-011 本身的判据闭环。守卫代码与注释均属另一进行中的工作,**本轮不触碰**。

### 3.3 台账其余活项(源码 ⛔ 无落点,引用台账)

UF-003(⬜ 等互通性决策)、UF-005a/b(⏸️)、DF-001~005(⏸️)—— 详见 `unfixed_defects.md`,不在本次源码注释处置范围内。

## 4. C 类:保留理由(142 处概览)

这些 ⛔ 删除后会产生真实回归风险,按域归纳:

| 域 | 代表 | 为什么必须保留 |
|---|---|---|
| 跨文件同步禁令 | `ipc.cpp`/`sniffer.cpp` 段名构造、`pool_attribution_benchmark` 段名副本、`test_pool_exhaust` 重置段名 | 名字不一致 = 静默另建段/重置失效,**编译器不会报** |
| 资源/锁纪律 | `ipc_info_pool.cc` 解锁后再诊断、不能直接调 `gc_dead`(自死锁)、`control_plane.h` open-only | 违反即死锁/跨进程阻塞/误删共享段 |
| 生命周期/顺序约束 | `socket_ser_cli_ipc.cc` 停机顺序、`auto_ser_cli_ipc.h` 路由互斥、`circularqueue.h` 回调设置时序 | 违反即 use-after-close/半发/数据竞争 |
| 兼容性契约 | `udp_id_init_msg.hpp` 帧判据(禁用 `size()>=4`、页尾陷阱)、F2 原因归因纪律 | 违反即新旧进程互通破裂/错误归因复发 |
| 数值判据纪律 | `data_rev.cc` 控制律硬禁令(不得自设迟滞、判别量不进 bps=、幅度逐字不动) | 全部是变更控制红线,动一个字就是改判据 |
| 量具方法论 | `test_pool_exhaust` 阳性/阴性对照自证、`pool_attribution` 连续采样/走链限步、`test_alloc_fault_inject` 命中计数/SKIP 纪律 | 违反则用例**假绿**(比没有测试更糟) |
| 修复后行为契约 | UF-002 族失效态判空(`shm.cpp`/`udp.cpp`/`sync/*`/`buffer.cpp`/`pool_alloc.h`)、UF-010 按内容哈希禁回退 | 描述的是**当前守卫为什么存在**,删除即诱发"优化"回退 |

## 5. 审计后动作与验证

- 删除 1 处整段、压缩 6 处叙事(`⛔` 标记随之消失,共减 7 处);其余 144 处原样保留。
- 全量重建 + 关键回归(adopt_loan_quota / loan / dzflat_transport)验证注释改动未破坏编译与行为。
- 本文档为 `⛔` 标记的现行语义权威说明:**新增 ⛔ 时只用于"违反即回归/假绿"的守则与活态限制,不得用作已修复缺陷的叙事**。
