# UF-011 证据登记(待 writer-claude 落笔进 unfixed_defects.md)

> ⛔ 本文件是**登记材料**, 不是台账本体。`docs/unfixed_defects.md` 是单写者协议, 实际条目由
> writer-claude 落笔。本文件只提供可机械核对的证据链。
>
> **建议台账行**: UF-011 `池空闲链二次入池` —— 状态建议 **🔶部分修**(不是 ✅已修, 理由见 §6)。

## 0. 一句话

`chunk->conns()` 是**位图不是计数**, 清它的地方有三处、由不同线程驱动, 而"谁有权还池"用的是
**位置判据**(位图现在空不空)而不是**动作判据**(这一位是不是我清掉的) ⇒ 同一块 chunk 会被
归还两次 ⇒ `id_pool::release` 头插时 `cursor_` 已等于 `id` ⇒ `next_[id] == id` 自环 ⇒
`acquire()` 永远返回同一块、池里其余 id 永久不可达。

修法 = 把归还判据换成动作判据(**每人只清自己那一位; 谁把位图清空, 谁还池**)。

## 1. 三方指纹

| 键 | 值 |
|---|---|
| `tree_head` | `90510a5` (dev; 工作树脏) |
| `src/libipc/ipc.cpp` | `fb37a18834d0eb23c27918a0344b8f1c` (refine) |
| `src/libipc/prod_cons.h` | `fbb5f001ec549cd5231d8490e06f1c31` (refine + 方案 A) |
| `test/test_uf011_chunk_return.cpp` | `fce1e8e4261076d73d67d6aab16a5ea2` |
| `include/libipc/def.h` | `4801d04d251cca491f3de89994bbc25f` (`large_msg_cache = 40`) |
| `build/lib/libipc.so.1.3.0` | `38d8c63d5fca1992d50cde322eb48726` |
| `build/bin/test_uf011_chunk_return` | `6dcd0038f4f9891eb1c83bb1531cd45e` |

⚠️ 以上取自**隔离沙箱** `/tmp/uf011_base`(工作树 rsync 副本), 因主树 `build/` 正被另一
Agent 占用重建。⛔ 沙箱构建物**不等于**主树构建物, 主树合入后须重取一次。

⚠️ **`ipc.cpp` / `prod_cons.h` 所在的库正被并发编辑**(另一 Agent 在做"池段名编码容量
`__C<cap>`")。本文件的 md5 是**快照**, 不是稳定指纹 —— 引用读数时必须连同 md5 一起引。

## 2. 缺陷复现(改动前 = `git show HEAD:src/libipc/ipc.cpp`)

判据: `test/test_uf011_chunk_return.cpp`(40 轮, 每轮"一读一落后"洪泛 + 收尾覆写两圈)。

| 臂 | 自环判据(40 轮) | 读数 |
|---|---|---|
| **基线**(HEAD 无守卫) | **红 8/8** | 收尾后 `walk_free()` 返回 `-2`(链带环), `self_loop()` 命中 `next_[id]==id` |
| **refine**(动作判据) | **绿 8/8** | 40 轮全程无自环 |

⇒ 判据**承重**: 同一二进制只换 `ipc.cpp` 一个文件, 红绿翻转。

构造为什么是"一读一落后"(不能简化): 触发需要**两条归还路径同时开火** ——
收方 A 边收边丢(`buff_t` 析构 ⇒ `recycle_storage`)、收方 B 全程不读(环满 ⇒ `force_push`
⇒ `discard_storage`)。实测单收方 / 多收方只建不读 **20 轮 0 次自环**; 一读一落后
**17/20 轮自环**。

## 3. 修复内容(一处函数, `src/libipc/ipc.cpp: sub_rc(broadcast)`)

```cpp
// 旧: 清掉 curr_conns & ~conn_id(接收时在连的**所有**人), 再看"清完空不空"
auto last_conns = curr_conns & ~conn_id;
... return (chunk_conns & last_conns) == 0;

// 新: 只清自己那一位; 清空者唯一 ⇒ 还池者唯一
if ((chunk_conns & conn_id) == 0) { *out_dup = true; return false; }  // 重复归还, 无权还池
auto nxt = chunk_conns & ~conn_id;
if (CAS(chunk_conns, nxt)) { *out_dup = false; return nxt == 0; }
```

`discard_storage`(写方覆写归还)同步加同一判据: 若 `(cur_conns & rem_cc) == 0` ——
一个位都不剩 —— 说明那些位早被清过, 本次无权还池。

新增诊断 `note_double_return(site, id, conn_mask)`: 首报 + 每 1024 次节流(体例照
`note_pool_exhausted`)。⛔ 它**只报不修** —— 报出来的是"上游把同一块交付了两次"。

**射程**: 只对 broadcast 生效。unicast 的 `sub_rc` 仍无条件返回 true —— 它的收方上限是 1、
写方永不替它清位, 位图只有一个驱动者 ⇒ 不存在二次归还。⛔ 若将来 unicast 也接入覆写/借样
归还, 必须补同款判据。

## 4. 邻接回归(沙箱, 全部 rc=0)

| 二进制 | 结果 |
|---|---|
| `test_chunk_hold` | 3/3 |
| `test_lap_safety` | 3/3 |
| `test_uf007_id_pool_double_release` | 2/2 |
| `test_pool_exhaust_observability` | 2/2 |
| `test_loan` | 10/10 |
| `test_dzflat_transport` | 9/9 |
| `test_dzflat_rx` | 8/8 |
| `test_dzflat_sercli` | 4/4 |
| `test_uf011_chunk_return` | 1/1 |

## 5. 本轮**被实测证伪**的假设(八条, 逐条留档)

这一节比结论更重要 —— 每一条都曾看起来"显然成立", 而实测把它推翻了:

1. **"单收方就能复现"** —— 20 轮 0 次。必须两条归还路径同时开火。
2. **"纯落后(全不读)就能复现"** —— 20 轮 0 次(1 个和 3 个收方都试过)。
3. **"洪泛期间边发边采样更灵敏"** —— 反了: 采样要 fopen/fread 段文件, 拖慢发布方、改变
   竞态窗口 ⇒ 48 轮 0 次。**量具改变了被测对象**。改为只在轮末采样后立刻 8/8 红。
4. **"守卫把自环换成了泄漏(两个都要治)"** —— 部分成立: 先测到的 `free = 36~39/40` 里混着
   **收尾不干净**(见 6)与真泄漏; 分开量之后, 自环那一条已被完整闭合。
5. **"断开收方后再灌小消息就能把在飞的收回来"** —— 错。`push`/`force_push` 开头就是
   `if (cc == 0) return false;` ⇒ 一个收方都没有时**根本不落笔**, 覆写不发生。必须挂一个
   **新**收方再覆写, 否则量到的是"环里还剩多少在飞"而非"池漏了多少"(两者同形)。
6. **"量具门放每轮循环里更稳"** —— 错: 泄漏逐轮累积, 那时量具门读到 `cap−2` 是"又漏了一轮"
   不是"量具坏了"。守卫臂两次红全是量具门, 自环判据一次都没机会说话。量具门只能跑一次、
   且必须在池干净时跑。
7. **"每轮建两个收方就能复现"** —— 两个收方**都**持有时环满, 但只有一个在丢 ⇒ 退化形态。
   有效构造必须是一读一落后。
8. **"修完应该池永远回满"** —— 不是。见 §6。

## 6. ⛔ 未闭合的残余(为什么只能记 🔶部分修)

**(a) 归还得太早 ⇒ ABA(不在本次射程)**
`pop()` 先拷数据、后才清自己的 `rc` 位, 所以"已读到 storage id 但尚未清位"的收方会被写方
算进 `rem_cc`, 被当成"永远看不到这格"而**提前收回** —— 那个收方随后拿一个已回池(可能已复用)
的 id 去建 `buff_t`。本次改的是"同一块被还两次", **没有**碰这一条。
那一侧的防护在 `prod_cons.h` 的 `pop()` 里(拷贝前后与进 clear 前各比一次 epoch, 已落码)。
⇒ 判据与修法各管一头, ⛔ 不得把本项读成"撕裂窗已修"。

**(b) 偶发、位置不固定的 `free = cap−1`(未定位)**
同一构造连跑两次: 一次只在末 3 轮出现, 另一次 39/40 轮都出现 ⇒ **不可复现**, 已按"未定位"
登记。用例默认只**登记**这个读数, 不作门(`UF011_EXPECT_LEAK_FREE=1` 可把它变成硬判据用于
继续追查)。

**(c) `id_pool::release` 本身仍不幂等**
本次是在**调用方**保证幂等, `id_pool` 没有加任何守卫(加了就是布局变更, 要过互通性拍板)。
⇒ 任何**新增**的归还路径都必须自己带上这个判据, 否则同样的自环会从新路径回来。

## 7. 文档订正(本轮顺带, 已改在 `src/libipc/ipc.cpp`)

`discard_storage` 上方原写"**需在 pop() 侧增加覆写检测才能根除**" —— **陈旧**:
`prod_cons.h` 的 `pop()` 里那两道检测(② 拷贝前后比 epoch、③ 进 clear 前再比一次)与这段
注释**同一个提交**(`185dff9`)落地。`docs/dzflat_known_issues.md:285` 与
`docs/shm_chunk_pool_occupancy_plan.md:430` 都已按"窗口已关"记载, 只有源码注释没跟上。
本轮把它改成"本函数只关了一半: 拦的是重复归还, 拦不住归还得太早(见 6a)"。

---

## 8. 仪化: "读方拷贝"与"写方落笔"重叠(方案 A 那扇窗)

> 2026-09-21。此前这条只是**推演**(见 §6a 与上文"A 关的是哪扇窗"), 本轮把它变成了读数。

**构造**: 先只发不读把收方甩到落后 >256 槽 ⇒ 下一次 `pop()` 触发重同步
(`prod_cons.h` 把游标夹到 `cursor() - kRingSlots`, 而 `index_of(cursor()-256) ==
index_of(cursor())` ⇒ **落地那一格就是写头**)。随后一边持续发、一边排空, 3 秒 / 约 590 万条。

**仪化点**(插桩构建, 仅在沙箱; 判据见下):
- 写方: 在 `f(&el->data_, rem_cc)` 两侧写入 `g_writing = 本槽 index` / `kNone`;
- 读方: 拷贝前后各读一次 `g_writing`, 命中本槽 ⇒ `g_overlap++`;
  若同时 `(rc1 & ~ep_mask) == (rc0 & ~ep_mask)`(即 **② 的 epoch 比对没响**)⇒ `g_overlap_blind++`。

| 读数 | 不跳过写头 | 跳过写头(A′) |
|---|---|---|
| `g_head_read`(读到写头那一格) | 3148 | 5 / 12 / 103 |
| `g_overlap`(拷贝与落笔重叠) | 1360 | 0 / 6 / 20 |
| **`g_overlap_blind`(②③ 看不见的重叠)** | **1096** | **0 / 0 / 0** |

**非退化门**: `g_head_read > 0` —— 遭遇路径**真实存在**, 不是理论。(两次独立运行同号。)

**结论**: 重叠里 **81%(1096/1360)** 是 epoch 检查**看不见**的。机制是 `push` **先声明后落笔**:
`rc_` 的 CAS 在 `f(&el->data_, rem_cc)` **之前**(`prod_cons.h:267` vs `:273`), 所以若收方读到的是
CAS **之后**的值, epoch 在它拷贝**之前**就已经变了 ⇒ ②③ 全程沉默, 而载荷可能正被逐字节改写
⇒ 收下一个撕裂的 `storage_id`(指向池里另一条消息的 chunk)。⭐ 对照: **mmb 是反的** ——
`f()` 之后才 `f_ct_.store(...)`(`prod_cons.h:505-507`)。

**仪化可撤销性**: 插桩只加在沙箱。撤销后 `prod_cons.h` 回到 `7e90f14c847ff62b69c655863ff3b831`、
`ipc.cpp` 回到 `fb37a18834d0eb23c27918a0344b8f1c`, 与插桩前**逐位一致**;
`grep -c uf011_probe` 两者均为 **0**。

## 9. 方案 A 落码(`prod_cons.h`, +27 行)

⛔ **不是"改重同步常数"** —— 那个形态是错的, 本轮推翻:
`>` 改 `>=` 不移动落点(仍在写头); 重同步目标改成 `cursor() - kRingSlots - 1` 会落到
`index_of(cursor()) - 1` = **最新那一格**, 把投递顺序打乱。写头那格**就是**环里最旧的一格,
"换个地方重同步"解决不了它。

**正解 = 不读写头那一格**:

```cpp
if (circ::index_of(cur) == circ::index_of(cursor())) { ++cur; continue; }
```

| 项 | 结论 |
|---|---|
| 关窗判据 | `g_overlap_blind` **1096 → 0**(5 次运行全 0; 残余 `g_overlap` 6~20 次由 ② 接住) |
| 邻接回归 | `test_chunk_hold` 3/3, `test_lap_safety` 3/3, `test_uf007` 2/2, `test_pool_exhaust` 2/2, `test_loan` 10/10, `test_uf011` 1/1, `test_dzflat_transport` 9/9, `test_dzflat_rx` 8/8, `test_dzflat_sercli` 4/4 |
| 自环判据 | 6/6 绿 |
| 同负载速率(ABAB ×3) | A′ **3/3 对更快**(收 3.49/2.93/3.16M vs 2.46/2.22/1.83M, 区间不重叠)—— 跳过写头避免了与写方争同一条 cache line |
| ⛔ **仓定吞吐判据(P-2 ≥85.3 msgps)** | **未测**。机器正被另一 Agent 占用, `ipc_benchmark` 反而是 30 msg/s 且全 fallback 的延迟档, 不是该判据。⇒ **合入前必须补跑** |

**代价(必须随修法一起登记)**: 被套圈的收方每绕一圈**多丢一条**(原先丢 256 条里靠前的那些,
现在连最旧那条也不读)。符合 `force_push` 注释早已声明的契约"慢订阅者保持连接、只丢失被
套圈的消息", ⛔ **不是新语义**, 但行为确实变了, 不得当"无副作用"记。

## 10. 方案 B 的状态(未做, 等拍板)

B 要解决的是**另一扇窗**: 位图只记得"谁还没放手", **不记得"谁已经取走"**, 于是写方按 `rem_cc`
清位时无法区分"没取过"与"已取走但没放手" ⇒ 归还权同时落在两个人手上(实测指纹
`site = recycle_storage, conn_mask = 1`)。

正解 = 给槽位加**每收方已消费位图**(32 位足够)。这是 **wire 级**: `elem_size` 变 ⇒
`block_size` 变([elem_array.h:31-32](../src/libipc/circ/elem_array.h#L31-L32)), 而段按
`sizeof(Elems)` 打开([queue.h:37](../src/libipc/queue.h#L37))⇒ **同一话题**的新旧进程环段大小
不一致。**可借用的先例**: 本轮另一 Agent 正在给 chunk 段名加容量分量 `__C<cap>` 来隔离布局
不同的段 —— 同一手法可套到环段名上。⛔ 未拍板前不动。

## 11. 顺序与依赖

A 与 B **各关一扇窗, 不互为兜底**: A 关"载荷与声明不同步"(撕裂读), B 关"归还权不唯一"。
两者都改 `prod_cons.h` / `ipc.cpp` 同一批函数 ⇒ **必须串行落**, 不得两人并行改。
建议: A 已落(本轮) → 补跑 P-2 吞吐门 → 再议 B(等互通性拍板)。

⛔ **两者各自要留独立变异臂**: 否则会出现"B 做完 A 的用例也绿、A 其实不承重"的假象。
